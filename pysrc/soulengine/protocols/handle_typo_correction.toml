protocol_name = "Handle User Typos"
protocol_handle_name = "handle_typo_correction"
protocol_description = "When user messages are likely typos, ask for clarification"
trigger_prompt = "User message contains non-sensical words"
protocol_result = "Ask user to clarify their request"
apps_used = ["[codex_app]"]

[[step]]
id = 1
label = "Check message for typos"
prompt = "Is the user message likely a typo?"
completion_check_condition = "Yes"
